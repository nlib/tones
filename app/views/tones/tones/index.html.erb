<p id="notice"><%= notice %></p>

<h1>Listing Tones</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Short</th>
      <th>Body</th>
      <th>Lang</th>
      <th>Description</th>
      <th>De short</th>
      <th>De body</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tones.each do |tone| %>
      <tr>
        <td><%= tone.name %></td>
        <td><%= tone.short %></td>
        <td><%= tone.body %></td>
        <td><%= tone.lang %></td>
        <td><%= tone.description %></td>
        <td><%= tone.de_short %></td>
        <td><%= tone.de_body %></td>
        <td><%= link_to 'Show', tone %></td>
        <td><%= link_to 'Edit', edit_tone_path(tone) %></td>
        <td><%= link_to 'Destroy', tone, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Tone', new_tone_path %>
